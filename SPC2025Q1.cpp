#include<bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n, m;
    cin >> n >> m;
    
    if (m>=(n+1)/2) cout << "nEWBIE\n";
    else cout << "PRO\n";
    
    return 0;
}
